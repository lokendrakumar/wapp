var apps = module.exports = [
  {prefix: '/', app: require('./admin')},
  // {prefix: '/dashboard', app: require('./dashboard')},
  // {prefix: '/widgets', app: require('./widgets')},
  // {prefix: '/recorder', app: require('./recorder')},
  // {prefix: '/campaigns', app: require('./campaigns')},
  // {prefix: '/', app: require('./newsite')},
  //{prefix: '/', app: require('./site')}
];
